# Zenit-FE
This project is the frontend component of the Zenit project – a prototype platform for training pilots in small aircraft cockpits.

# Frontend Launch
Install project dependencies:
```bash
$ yarn install
```

Run the application:
```bash
$ yarn start
```

Frontend app on `http://localhost:3000`